/**
 * Конфигурация Tailwind CSS для сборки статического файла стилей.
 * Сборка: npm run build:css  →  assets/css/tailwind.css (файл коммитится в репозиторий,
 * поэтому для публикации на хостинге Node.js и сборка не нужны).
 *
 * @type {import('tailwindcss').Config}
 */
module.exports = {
    // Файлы, в которых Tailwind ищет используемые классы.
    // Важно: assets/js тоже сканируется, потому что классы встречаются в шаблонных строках.
    content: ['./index.html', './404.html', './assets/js/**/*.js'],

    theme: {
        extend: {
            fontFamily: {
                sans: ['Roboto', 'system-ui', '-apple-system', 'Segoe UI', 'Arial', 'sans-serif'],
            },
            colors: {
                // Зелёная палитра турнира
                primary: {
                    50: '#e8f5e9',
                    100: '#c8e6c9',
                    200: '#a5d6a7',
                    300: '#81c784',
                    400: '#66bb6a',
                    500: '#4caf50',
                    600: '#43a047',
                    700: '#388e3c',
                    800: '#2e7d32',
                    900: '#1b5e20',
                },
                // Нейтральная (серая) палитра
                dark: {
                    50: '#f8f9fa',
                    100: '#f1f3f4',
                    200: '#e8eaed',
                    300: '#dadce0',
                    400: '#bdc1c6',
                    500: '#9aa0a6',
                    600: '#80868b',
                    700: '#5f6368',
                    800: '#3c4043',
                    900: '#202124',
                },
            },
            // Исправление: раньше в разметке использовался несуществующий класс
            // border-dadce0, и рамки получали случайный цвет. Задаём фирменный цвет по умолчанию.
            borderColor: {
                DEFAULT: '#dadce0',
            },
        },
    },

    plugins: [],
};
